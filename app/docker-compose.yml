version: '3.8'

services:
  gptlite:
    build: .
    ports:
      - "3000:3000"
    environment:
      - OPENAI_BASE_URL=
      - OPENAI_API_KEY=
      - AZURE_OPENAI_BASE_URL=
      - AZURE_OPENAI_DEPLOYMENT=
      - AZURE_OPENAI_API_KEY=
